import 'package:flutter/material.dart';

class Useinputtextfeild extends StatelessWidget {
  const Useinputtextfeild({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Column(children: [],),);
  }
}